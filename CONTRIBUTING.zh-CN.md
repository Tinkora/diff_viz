# 贡献指南

## 范围

保持 `diff_viz_core` 平台无关。浏览器代码必须调用 core，不得重复输入校验或输出逻辑。只有在存在具体用例和测试时，才添加新的 transport 或托管服务。

## 开发流程

1. 创建聚焦的分支或 worktree。
2. 在改变行为前，先写一个面向结果且会失败的测试。
3. 编写能让测试通过的最小实现。
4. 运行 `README.zh-CN.md` 中记录的检查，并确保生成物不进入提交。
5. 不要提交生成的 `target/`、`pkg/`、`node_modules/` 或 Playwright 产物。

修改浏览器界面时，应运行浏览器 smoke 检查，并实际检查 375、768、1024 与 1440 px 下的渲染布局、键盘操作、焦点状态、减少动态效果和横向溢出。

## 变更要求

- 保持提交聚焦，并描述可观察行为。
- 保持 schema 字段名和错误 code 稳定；有意改变兼容性时必须记录。
- 命令、成熟度、隐私边界或限制改变时，同时更新两种语言的 README。
- 不要加入 secrets、凭据、私有输入或生成的构建产物。
