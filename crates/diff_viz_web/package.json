{
  "name": "diff-viz-web-smoke",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build:wasm": "wasm-pack build --target web --out-dir static/pkg --out-name diff_viz_web . -- --locked",
    "prepare:wasm-smoke": "node scripts/prepare-wasm-smoke.mjs",
    "test:wasm-smoke": "npm run prepare:wasm-smoke && playwright test",
    "test:wasm-smoke:local": "npm run build:wasm && npm run test:wasm-smoke"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "http-server": "14.1.1"
  }
}
